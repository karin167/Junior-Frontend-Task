import { render, screen } from "@testing-library/react";
import App from "./App";

test("renders learn react link", () => {
  render(<App />);
  // const appDiv = document.getElementsByName("div");
  // console.log(appDiv);
  // expect(document.getElementById("appDiv")).toBe();
});
